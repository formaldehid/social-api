# Proto definitions

These protobuf files are included as a bonus artifact to demonstrate how the service can evolve toward API-to-API gRPC communication without changing the domain layer.

The running application still uses HTTP for external dependencies. The proto files model the same contracts behind transport-neutral service boundaries.
